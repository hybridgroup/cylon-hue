"use strict";

var Hue = lib("adaptor");

describe("Cylon.Adaptors.Hue", function() {
  var adaptor = new Hue();

  it("needs tests", function() {
    expect(adaptor).to.be.instanceOf(Hue);
  });
});
